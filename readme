Como colocar o codigo no Github

Git init
Git brunch -m master main
Git config --global.user "usuario"
Git confif --global.email "email"
Git add .
Git Commit -m "nome do save"
Git remote add origin main "url"
Git push -u origin main